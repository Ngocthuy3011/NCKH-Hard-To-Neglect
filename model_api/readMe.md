# Hướng dẫn của Thành Nhân
## Cách chạy API đăng kí khuôn mặt
```
python -m APIFaceRegister.py
```
## Push code lên githud
```
git add .
git commit -m "Khoi tao du an"
git branch -M main
git push -u origin main
```
## Pull code về
```
git pull origin main
```